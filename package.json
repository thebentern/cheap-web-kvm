{
  "name": "cheap-web-kvm",
  "private": true,
  "description": "Build tooling for the web UI stylesheet. Not needed to run the app - app.css is committed.",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "build:css": "tailwindcss -i tailwind.input.css -o app.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0",
    "tailwindcss": "^4.0.0"
  }
}
